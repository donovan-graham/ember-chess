import { toDash } from '../../../helpers/to-dash';
import { module, test } from 'qunit';

module('Unit | Helper | to dash');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toDash(42);
  assert.ok(result);
});
